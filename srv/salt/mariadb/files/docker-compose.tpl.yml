version: '3.3'

services:

  db:
    image: mariadb:latest
    ports: 
      - "3306:3306"
    volumes:
      - 'db:/var/lib/mysql'
    environment:
      - MYSQL_ROOT_PASSWORD=Cyb3rR@nger

volumes:
  db: