version: '3'

services:
  radarr:
    image: linuxserver/radarr:116
    ports:
      - 7878:7878
    volumes:
      - downloads:/downloads- ./config:/config
      - ./mnt/movies:/moviesenvironment:
      PUID: 1004
      PGID: 1004

volumes:
  downloads: