version: '3'

services:
  {% for project in salt['pillar.get']('hugo:projects', '') %}
  {{project}}-webserver:
    image: roquefort/hugo:latest
    build: .
    ports:
      - 13{{ loop.index }}3:1313
    volumes:
      - ./src/{{project}}:/src
      - ./output/{{project}}:/output
    environment:
      WATCH: "true"
      REFRESH_TIME: 15
      BASEURL: "http://192.168.5.118"
  {% endfor %}