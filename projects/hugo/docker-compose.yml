version: '3'

services:
  
  backend-webserver:
    image: roquefort/hugo:latest
    build: .
    ports:
      - 1313:1313
    volumes:
      - ./src/backend:/src
      - ./output/backend:/output
    environment:
      WATCH: "true"
      REFRESH_TIME: 15
      BASEURL: "localhost"
  
  frontend-webserver:
    image: roquefort/hugo:latest
    build: .
    ports:
      - 1323:1313
    volumes:
      - ./src/frontend:/src
      - ./output/frontend:/output
    environment:
      WATCH: "true"
      REFRESH_TIME: 15
      BASEURL: "localhost"
  