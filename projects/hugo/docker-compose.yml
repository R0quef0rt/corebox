version: '3'

services:



  1

  frontend-webserver:
    image: roquefort/hugo:latest
    build: .
    ports:
      - 1313:1313

    volumes:
      - ./src/frontend:/src
      - ./output/frontend:/output
    environment:
      HUGO_WATCH: "true"
      HUGO_REFRESH_TIME: 15
