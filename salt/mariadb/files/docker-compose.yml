version: '3.3'

services:

  db:
    image: mariadb:latest
    ports: 
      - "3306:3306"
    volumes:
      - '/var/lib/mysql:/var/lib/mysql'
    networks:
      - backend
    env_file:
      - db.env

networks:
  backend: